<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link href="{{cdnLocal('/css/custom..css')}}" rel="stylesheet">
  <link href="{{cdnLocal('/css/bootstrap.min..css')}}" rel="stylesheet">
  <link href="{{cdnLocal('/css/fontawesome.min..css')}}" rel="stylesheet">
  <link href="{{cdnLocal('/css/materialdesignicons.min..css')}}" rel="stylesheet">
  <link href="{{cdnLocal('/css/materialdesignicons.css.map')}}" rel="stylesheet">

  <style>
    .carousel-item {
      height: 500px;
      /* Set the desired height of the slider */
      opacity: 0.3;
      transition: opacity 0.8s ease-in-out;
    }

    .carousel-item.active {
      opacity: 1;
    }

    .carousel-item img {
      object-fit: cover;
      width: 100%;
      height: 100%;
    }

    .carousel-caption {
      background-color: rgba(0, 0, 0, 0.7);
      /* Adjust the background color and opacity of the caption */
      color: #fff;
      /* Adjust the text color of the caption */
      padding: 10px;
    }

    .carousel-caption h3 {
      font-size: 24px;
      font-weight: bold;
    }

    .carousel-caption p {
      font-size: 18px;
    }
  </style>
  <link rel="icon" type="image/png" href="/assets/img/hamasa.png" />
  <title>Hamasa|Safaris</title>
</head>

<body>
  <!-- Navigation-->
  @include('inc/parksNavbar')
  <!-- Masthead-->
  <!-- About-->

  <!-- Projects-->
  @!section('content')
  <!-- Signup-->
  <!-- Footer-->
  @include('inc/footer')
  <!-- Bootstrap core JS-->

  <script src="{{cdnLocal('/js/jquery.min.js')}}">
  </script>
  <script src="{{cdnLocal('/js/bootstrap.min.js')}}">
  </script>
  <script src="{{cdnLocal('/js/custom.js')}}">
  </script>

</body>

</html>